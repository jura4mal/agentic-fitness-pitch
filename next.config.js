/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },
  // ❌ УБРАЛИ experimental.turbopack полностью
};

module.exports = nextConfig;